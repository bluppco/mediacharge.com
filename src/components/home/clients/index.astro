---

// IMPORTS ASTRO LAYOUTS
import Container from "@/layouts/container/index.astro"

// IMPORTS ASTRO ATOMS
import HeadingTwoWhiteCenter from "@/atoms/heading/two/white/center/index.astro"
import PictureContain from "@/atoms/picture/contain/index.astro"

// ASTRO PROPS
const {

    CDN,
    clients,
    data,
    PROJECT

} = Astro.props

---
<section class="bg-mc_black py-10 2xl:py-20 md:px-60">
    <Container>
        <HeadingTwoWhiteCenter>{ data.title }</HeadingTwoWhiteCenter>
        <section class="grid md:grid-cols-4 gap-x-4 gap-y-4 md:gap-y-10">
            {

                clients.map( ( value ) => {

                    return(
                        <div class="flex flex-col items-center gap-2">
                            <div class="h-20 w-full">
                                <PictureContain
                                    alternative_text={ data.title }
                                    CDN={ CDN }
                                    image_width="200"
                                    PROJECT={ PROJECT }
                                    source={ value.cover }
                                />
                            </div>
                            <p class="text-white text-sm text-center">{ value.tag }</p>
                            <div class="pt-4">
                                <p class="text-white text-sm font-merriweather font-light text-center leading-relaxed">{ value.description }</p>
                            </div>
                        </div>
                    )

                })

            }
        </section>
    </Container>
</section>
